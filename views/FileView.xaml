<Window x:Class="MailClient.views.FileView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:fileview="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:file="clr-namespace:MailClient.Models"
        mc:Ignorable="d"
        Title="FileView" Height="450" Width="800">
        <Window.DataContext>
                <file:GetMailViewModel></file:GetMailViewModel>
        </Window.DataContext>
        <fileview:WebView2 Name="WebView" Source="{Binding SelectedFilePath}"/>
</Window>
